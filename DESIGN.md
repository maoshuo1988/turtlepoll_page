# DESIGN SYSTEM

本文件是项目 UI 风格规则。之后新增页面、弹框、卡片、表格、按钮、移动端适配，都要先按这里处理。

## 设计目标

整体风格：

- 现代化
- 极简
- 高级感
- 类似 Linear / Stripe / Vercel / Notion / Raycast / Apple
- 避免传统后台“土味 UI”

项目气质：

- 这是一个预测、龟币、宠物、开撕台、小游戏混合产品。
- UI 可以有轻微游戏感，但不能变成花哨页游。
- 主体验应像现代产品控制台，清爽、克制、可扫描。
- 宠物、世界杯、开撕台等主题页面可以有局部氛围，但整体仍要收敛。

## Project Visual Language

当前项目已有风格：

- 主背景：深黑 / 近黑，常见 `#080808`、`#090909`、`#0f1013`。
- 内容卡片：暗色卡片 + 浅透明边框，常见 `border-white/8`。
- 品牌主色：emerald / teal，用于 CTA、active、金币、成功态。
- 辅助强调：amber 用于引导、奖励、金币氛围；cyan/rose 可用于对阵双方或状态。
- 字色层级：白色标题、zinc/slate 灰正文、低透明说明文字。
- 组件观感：圆角、细边框、轻微阴影、局部 backdrop blur。

新增 UI 必须延续这些特征，不要突然切换成完全不同的设计语言。

## Layout

页面布局：

- 使用大量留白。
- 页面内容不要贴边。
- 卡片之间保持统一间距。
- 页面宽度不要过宽，主内容优先控制在舒适阅读宽度。
- 优先使用 8px 间距体系。
- 移动端先保证纵向流畅，再考虑桌面多列。

推荐间距：

- 8
- 12
- 16
- 20
- 24
- 32

禁止：

- 元素紧贴。
- 间距混乱。
- padding 不统一。
- 页面所有区域都塞满卡片。
- 为了炫技做过宽、过高、不可扫描的首屏。

## Desktop / PC

PC 端目标：

- 信息更完整，但不能拥挤。
- 充分利用横向空间，但主内容不要无限拉宽。
- 左侧栏、顶部栏、主内容区要有稳定层级。
- 适合重复操作、扫描数据、快速切换页面。

PC 端必须：

- 主内容区保留舒适内边距，推荐 24 / 32。
- 列表、表格、卡片网格使用 `grid` / `flex` / `gap` 管理间距。
- 侧栏导航只放主页面入口，辅助动作放业务卡片区域。
- 弹框桌面端优先居中，宽度克制，常用 420 / 520 / 640。
- 表格或记录类内容在 PC 端可以展示更多列，但操作区保持靠右。
- 页面最大宽度要根据内容控制，避免大屏下文本行过长。

PC 端禁止：

- 把移动端单列直接拉满大屏。
- 一屏堆太多高亮卡片。
- 大量固定宽度导致小屏笔记本溢出。
- 过度依赖 hover 才能发现关键操作。

## Mobile

响应式必须：

- mobile first。
- 平板适配。
- 不允许横向滚动。
- 触摸目标至少接近 40px。
- 底部弹框考虑 safe area。
- 移动端优先单列，桌面再升级为两列/三列。

手机端必须：

- 页面左右 padding 推荐 12 / 16。
- 重要操作按钮高度推荐 44 / 48。
- 表格在手机端改成卡片列表、横向摘要或分段信息，不强塞完整表格。
- 弹框手机端可使用底部 sheet，底部按钮区要避开安全区。
- 导航、筛选、记录折叠优先减少纵向噪音。
- 长标题必须换行或截断，不能压缩按钮。

手机端禁止：

- 桌面布局直接压缩到手机。
- 文字、按钮、卡片在手机上重叠。
- 固定宽度导致横向滚动。
- 依赖 hover 展示核心信息。
- 同一行塞入超过手机宽度可承载的信息。

## Card

卡片必须：

- 圆角。
- 微阴影。
- 边框浅色或低透明。
- 内容留白充足。
- 信息分组清晰。

推荐：

- `rounded-xl`
- `rounded-2xl`
- `border border-white/8`
- `border-neutral-200`
- `shadow-sm`
- 暗色卡片用 `bg-[#0f1013]`、`bg-[#101114]` 或现有 `rdark` token。
- 浅色卡片用 `bg-white`、`bg-slate-50`、`border-slate-100/200`。

禁止：

- 重阴影。
- 黑色硬边框。
- 强烈渐变。
- 卡片套卡片套卡片。
- 每个小信息都单独做成浮夸卡片。

## Typography

标题：

- `font-semibold`
- `font-bold` 只用于关键标题或数据。
- `tracking-tight`
- 标题层级最多 3 层。

正文：

- `text-sm`
- `text-neutral-600`
- 暗色下使用 `text-zinc-300` / `text-zinc-400`。

说明文字：

- `text-xs`
- `text-neutral-400`
- 暗色下使用 `text-zinc-500`。

数据：

- 金币、赔率、胜率等关键数字可以更粗，但只突出真正重要的数据。
- 不要整页都是 `font-black`。

禁止：

- 超多字体大小。
- 五颜六色文字。
- 粗体滥用。
- 负 letter spacing 滥用。
- 大段文字居中堆叠。

## Color

整体颜色：

- 白色。
- 黑灰。
- 少量品牌色。

项目主色：

- 主 CTA / active / 成功：emerald。
- 金币、奖励、引导：amber 只能少量使用。
- 对阵或走势：cyan / rose 可以成对出现，但要控制面积。
- 错误：rose/red，只用于错误和风险。

品牌色只用于：

- CTA。
- active。
- 链接。
- 关键状态。
- 重要数据强调。

禁止：

- 高饱和颜色大面积铺底。
- 彩虹色。
- 紫色 AI 风格。
- 一个页面超过 2 个主强调色。
- 无意义渐变泛滥。

## Light / Dark Mode

项目必须同时考虑亮色和暗色。当前主应用更偏暗色，但新增组件不能只在暗色好看。

暗色模式：

- 页面背景：`#080808`、`#090909`、`#0f1013` 或现有 `rdark` token。
- 卡片背景：`#0f1013`、`#101114`、`dark:bg-rdark-card`。
- 输入背景：`#111215`、`#141518`、`dark:bg-rdark-input`。
- 边框：`border-white/8`、`dark:border-rdark-border`。
- 主标题：`text-white`、`dark:text-rdark-text`。
- 正文：`text-zinc-300` / `text-zinc-400`。
- 说明：`text-zinc-500`、`dark:text-rdark-text2`。
- hover：使用轻微白色透明层，如 `hover:bg-white/[0.04]`。

亮色模式：

- 页面背景：`bg-white`、`bg-slate-50`、`bg-neutral-50`。
- 卡片背景：`bg-white`。
- 输入背景：`bg-white` 或 `bg-slate-50`。
- 边框：`border-slate-200`、`border-neutral-200`。
- 主标题：`text-slate-900`、`text-neutral-900`。
- 正文：`text-slate-600`、`text-neutral-600`。
- 说明：`text-slate-400`、`text-neutral-400`。
- hover：使用 `hover:bg-slate-50` 或 `hover:border-slate-300`。

双主题要求：

- 新组件必须同时写好 `dark:` 样式，除非所在区域明确永远只有一种主题。
- 不允许暗色下低对比、亮色下发灰看不清。
- 不允许只用透明白色边框却没有亮色对应边框。
- 品牌色在双主题下都要保持可读：亮色可用 `text-emerald-600/700`，暗色可用 `text-emerald-300/400`。
- 状态色要有背景和文字的对比组合，不只改文字颜色。

## Buttons

按钮规则：

- 主按钮突出，优先 emerald。
- 次按钮弱化，使用透明/浅灰背景。
- 危险按钮使用 rose/red，必须有明确语义。
- 图标按钮必须有清晰 hover 态。
- 按钮高度统一，常用 36 / 40 / 44 / 48。

禁止：

- 同一区域多个主按钮同时抢焦点。
- 没有 hover / disabled / loading 态。
- 文字溢出按钮。

## Table

表格规范：

- 表格必须有 hover。
- 表头加浅背景。
- 行高舒适。
- 操作按钮靠右。
- 数字列右对齐或视觉上对齐。
- 空态要明确。

禁止：

- 密集型表格。
- 强边框。
- 表格里塞过多按钮和图标。

## Form

表单规范：

- label 在上方。
- 输入框高度统一。
- 间距统一。
- 错误提示明显。
- 金额输入必须有单位或上下文。
- 提交前的重要操作必须弹框确认。

按钮：

- 主按钮突出。
- 次按钮弱化。

禁止：

- 没有错误态。
- 只靠 placeholder 当 label。
- 未登录或余额不足时仍允许提交。

## Modal

弹框规范：

- 下注、登录、引导、确认操作优先用弹框。
- 已有弹框必须优先复用。
- 移动端弹框可从底部出现，桌面端居中。
- 遮罩不能太浅，避免背景干扰。
- 弹框最大宽度要克制，常用 420 / 520。

项目约定：

- 暗盘下注复用 `PredictionBetModal` 风格。
- 开撕台下注参考暗盘下注风格。
- 新手引导只使用 `GuideTourModal`。
- 龟仙人聊天只使用 `PetChat`。

## Interaction

动效必须：

- 快。
- 轻。
- 不花哨。
- 有操作反馈。

推荐：

- `transition-all`
- `transition-colors`
- `duration-200`
- hover 轻微背景/边框变化。
- 重要卡片可轻微 `hover:-translate-y-0.5`，不要滥用。

禁止：

- 弹跳动画。
- 复杂动画。
- 过长动画。
- 滚动时大量动画干扰。
- 所有元素都动。

## Icons

图标规则：

- 优先使用 `lucide-react`。
- 图标只用于辅助理解，不要填满页面。
- 导航、按钮、状态标签可以使用图标。
- 表格和密集列表少用图标。

禁止：

- 太多 icon。
- 不同风格图标混用。
- 没有文字语义的陌生图标。

## AI UI 禁止项

严禁：

- 渐变泛滥。
- 紫色 AI 风。
- 卡片堆满屏幕。
- 超大阴影。
- 元素拥挤。
- 过度边框。
- 太多 icon。
- 信息密度爆炸。
- 所有区域都强调。
- 花哨图表。
- 大面积发光边框。
- 无意义装饰圆球、光斑、bokeh。

## Feature-Specific Rules

预测 / 暗盘：

- 信息优先级是标题、选项、赔率、参与度、下注入口。
- 下注入口必须明确、稳重，避免误触。
- 金额、余额、预计收益要清晰。

开撕台：

- 可以有更强对抗感，但控制颜色面积。
- 双方颜色可以使用 cyan/rose，对比要稳定。
- 历史战绩、赛季记录应可扫描，不要做成大段文字。

宠物：

- 可以更轻松，但不要过度卡通化。
- 龟种能力、等级、稀有度要结构化展示。
- 装备按钮使用项目主色，不能突兀。

世界杯：

- 可以使用绿茵、金杯、赛程氛围。
- 不要让赛事主题破坏项目整体暗色高级感。
- 大图/视觉氛围只服务内容，不做纯装饰堆叠。

## Preferred Style

优先参考：

- Linear
- Vercel
- Stripe Dashboard
- Raycast
- Notion
- Apple

最终判断标准：

- 一眼高级。
- 操作清楚。
- 信息可扫描。
- 颜色克制。
- 移动端不崩。
- 和当前项目已有 UI 像同一个产品。
