# 选龟界面大改 — Layer A 视觉重构 (Team Select Revamp)

> 里程碑: **V3**(商业质感) ｜ 分类: 体验打磨/美术 ｜ GDM 挂: UI/UX
> 范围决策(2026-05-27 用户拍板): **只做 Layer A 视觉重构**, 不动数据/战斗; 技能 5选3 **整合进右侧详情面板**(替掉点被动图标弹窗)。
> 源参考图: 用户提供的目标稿(三栏 + 队伍加成 + 常驻详情面板 + 清空/自动布阵)。传 HacknPlan Storage 附此 item。

## 现状 → 目标 (gap)
**现状**(`src/scenes/TeamSelectScene.ts`, DOM overlay): 标题 + 模式提示 + 前/后排简单槽 + 返回/上次阵容/确认 + 羁绊预览 + 筛选条 + 龟卡网格; 点被动图标→弹窗选技能。
**目标**: 三栏布局 — 左队伍加成 / 中阵型(大槽+清空+自动布阵) / 右常驻详情面板(立绘+属性+技能5选3+开始冒险) + 底部龟卡网格(元素图标+星级)。

## Layer A 子任务 (全 P1, 逐个一提交)
1. **三栏布局骨架** — 重排 `#poc-team-select-root` 为 左/中/右 三栏 + 底部网格; 标题改「选择你的伙伴」+ 副标题; 保留 FIT 缩放 + 现有 DOM 刷新管线(refreshDom 拆分到各栏)。
2. **中栏 阵型区** — 前排(1/2/3)/后排(4/5/6)大槽 + 装饰编号标签 + 剑/盾占位图标; 复用现有 drag/drop + onSlotClick; 顶部加 **清空**(team 全清) + **自动布阵**(自动填 3 只: 取已选优先, 否则按当前排序前 3)。
3. **右栏 常驻详情面板** — 点龟卡/槽位 → 更新: 立绘(底座 diorama)/名+稀有度徽章/星级/描述/属性行(生命/攻击/防御/暴击率; 速度见下; **元素行已去掉**)/**技能 5选3 勾选区**(复用 getAvailableSkillIndices + writeLoadout, 替掉 openSkillPicker 弹窗)/**开始冒险 CTA**(= 现 confirm, disabled 逻辑不变)。
4. **左栏 队伍加成面板** — Layer A 复用 `calcActiveSynergies` 展示**当前激活羁绊**(套参考图左面板样式); 五维百分比(生命/攻击/防御/速度/暴击率)行先**占位/隐藏**, 标注待 Layer C 队伍加成公式填。含 ? 帮助图标。
5. **龟卡重做** — 卡片加 **星级**(底部); 保留稀有度徽章/Lv/拖拽/选中态。(元素图标已去掉)
6. **样式/动效收口** — 选中态高亮、hover、CTA 发光; 移动端窄屏三栏退化(右栏可抽屉化, 待眼验)。

## 新字段 Layer A 映射 (纯展示, 不入战斗)
- **星级 ★** ← 稀有度映射(默认: C..SSS → 比例填 5 星; 具体档位实现时定, 可调)。
- **元素** — ❌ 用户决定去掉(卡片图标 + 详情面板元素行都不做)。
- **速度** ← 无 `speed` 字段。Layer A: 属性行**先隐藏或显占位 "—"**(队伍加成同); 真值待 Layer C(影响出手序)。
- **队伍加成五维 %** ← 待 Layer C 公式; Layer A 左面板先以羁绊为主体内容。

## 不做 (留 Layer B/C, 已记 PM-BOARD)
- Layer B: 给 28 龟正式补 `element`/`stars` 数据字段 + 元素图标资产正式化。
- Layer C: 速度真实参与出手序、元素克制、队伍加成公式入战斗结算(动战斗逻辑, 要先开设计)。

## DoD
typecheck 过 + Playwright 验(布局不崩/点卡片右栏更新/5选3写入 loadout/确认开战可达) + 移动端窄屏眼验 + 一子任务一提交 + push。
